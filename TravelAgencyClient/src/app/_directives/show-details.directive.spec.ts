import { ShowDetailsDirective } from './show-details.directive';

describe('ShowDetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowDetailsDirective();
    expect(directive).toBeTruthy();
  });
});
